# Railway config-as-code (n8n-main)
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ALWAYS"
numReplicas = 1
sleepApplication = false
deploymentTimeout = 600


